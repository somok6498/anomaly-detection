package com.bank.anomaly.config;

import org.springframework.boot.test.context.TestConfiguration;

/**
 * Placeholder — Aerospike beans removed after Oracle migration.
 * Retained so any @Import(TestAerospikeConfig.class) references still compile.
 */
@TestConfiguration
public class TestAerospikeConfig {
}
